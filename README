
To run this:

1. Open wsl in the folder where the project repo has been cloned
2. Run: make clean
3. Run: make
4. Run: make qemu-nox

5. Once in qemu, execute
    i) cat README
    ii) ls
    iii) echo hello > sample.txt
    iv) grep hello sample.txt
    v) showlogs

6. showlogs will only display 10 entries at a time
7. Optionally, execute one command and showlogs right after
